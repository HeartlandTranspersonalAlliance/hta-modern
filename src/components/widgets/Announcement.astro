---
import { getPermalink } from '~/utils/permalinks';
---

<div class="hidden md:flex justify-center px-3 pt-3">
  <a
    href={getPermalink('/initiatives')}
    class="hta-focus-ring paper-panel px-4 py-2 text-sm font-semibold text-muted hover:text-[var(--hta-link-hover)] transition md:text-base"
  >
    <span class="mr-2 font-heading uppercase text-[var(--hta-link)]">HTA</span>
    Community, Education & Harm Reduction in KC
  </a>
</div>
